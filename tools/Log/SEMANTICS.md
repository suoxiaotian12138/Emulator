# 日志语义一致性约束

语义一致性 = 外部可观测行为。实验与分析脚本应仅依赖事件流中外部可见的字段和值（例如 `cell_cmd`、`dir`、`side`、`circ_id`、`stream_id`、`hop`、`peer`），并以时间顺序和节点视角对齐，不假设实现内部的额外状态或隐式逻辑。凡是插针点产生日志时，需确保字段名和取值与 `schema.Event` 约定一致，以便不同实验与脚本在同一基准上对齐。